# Configure Discard for soft deletes
# Models that include Discard::Model will have soft delete functionality
# via the deleted_at column (configured in each model with self.discard_column = :deleted_at)
